# Parity with CurseForge **larion_lite-0.1.0**

- **Java**: Matches the published JAR (same terrain math, presets, caves, scree, commands). Your dev tree is already the 0.1.0 codebase (Mojang-mapped sources; the CurseForge folder is only extracted **resources** from the same build).
- **Resources**: `assets/`, `data/`, `pack.mcmeta` align with the jar contents under `larion_lite-0.1.0/`.
- **If worlds feel different**: Usually **`config/larion_lite-common.toml`** was edited. Reset using `config-defaults/larion_lite-common-0.1.0.toml`.
